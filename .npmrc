# Silence the React 19 RC peer warning from next@15.0.0 — harmless at runtime
# (Next 15 was pinned to a React 19 RC build; stable React 19 works fine).
# Bump next to 15.0.3+ later to remove this.
auto-install-peers=true
strict-peer-dependencies=false
