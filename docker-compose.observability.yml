# DEPRECATED — observability services are now consolidated into the main
# `docker-compose.yml` under the `observability` profile.
#
# Use `docker compose --profile observability up -d` to start them.
# This file is kept empty so older runbooks pointing at it don't break,
# but it has no services of its own.
